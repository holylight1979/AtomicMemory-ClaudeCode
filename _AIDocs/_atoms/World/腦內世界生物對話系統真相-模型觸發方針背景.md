# 腦內世界生物對話系統真相-模型觸發方針背景

- Scope: global
- Author: holylight
- Confidence: [臨]
- Trigger: 腦內世界, world.html, 生物對話, creature-chat, CHAT_MODEL, 接觸觸發, checkDialogue, sayPair, 對話方針, 背景電子腦, 模型退罐頭
- Created-at: 2026-06-17
- Related: 腦內世界-v3-自癒與-command-bus-架構, 腦內世界-環境演化-放置式架構, reconcile-render-動畫狀態歸屬陷阱

## 知識

- [臨] 對話 LLM 模型：world.html `const CHAT_MODEL` 必須是 rdchat-direct 後端(192.168.199.130:11434) `/api/tags` 內實存的模型名，否則 `/api/chat` 回 404 → world-chat.js 回 502 → 前端 `!r.ok` → **永遠退罐頭台詞**(且罐頭不入 _chatLog)。現用 `sorc/qwen3.5-claude-4.6-opus:4b`(4b 首次冷啟~25s)。換模型先 curl /api/tags 確認。
- [臨] 對話觸發(2026-06 #2 改制)：移除 dialogueDirector「寬鬆靠近/related 任意距離」憑空對話；改由 checkDialogue 掃「真接觸」(CONTACT_F=(a.s+b.s)²×0.55)觸發，同房漫步或跨區串門子(maybeCrossVisit)訪客接觸皆可；chatBusy 單一序列鎖(共用 3090)+ 每隻 CHAT_COOLDOWN=8s 冷卻防黏著；對話全程 talkFor 鎖到結束(含 9s LLM 逾時)→講完才動(eligibleForChat 檢 _talkUntil/_chatCdUntil)。
- [臨] 對話方針(creatureChat prompt)：personaOf 由對立性格→「守備範圍＝可分享經驗領域」；usr 主題輪替三選一：分享經驗/記憶、聊 AI 推導的興趣、**商量住的腦區「家」要蓋成/發展成什麼樣**(保留環境演化家園主題)；語氣溫和不互嗆。罐頭備援同步改寫。
- [臨] 待做 #3：多輪上下文接續(對話 session 狀態機 {a,b,history,turn} 3~4 句、history 入 prompt 讓 LLM 接續、整段往返入雙方 _chatLog(現只存最後一句)、依關係給話題種子)。架構決策：不拆檔，同檔收斂成 const Dialogue={...}；門檻 >300~400 行或跨 session 持久化才拆 ES module。
- [臨] 背景 drawBg：程序生成「巨大電子腦」(神經元聚成腦輪廓 BRAIN ellipse + 腦溝波浪 + 突觸脈衝)+ Anthropic 暖陶橘 ACCENT="217,119,87"(#D97757)致敬向配色 + 放射 spark(非精確商標)。旋鑺d：ACCENT/節點數/BRAIN 半徑/alpha。要放真 logo 疊 SVG 在 #bg。

## 行動

- 改對話模型 → 先 curl 後端 /api/tags 確認模型存在，再改 world.html CHAT_MODEL
- 改對話觸發/方針 → 動 checkDialogue/sayPair/creatureChat/personaOf；改完 Ctrl+F5(world.html 是 file:// 單檔，免重啟 server)
- 續做 #3 多輪上下文 → 同檔 Dialogue 模組，勿拆檔
- 背景調色/改形 → 改 drawBg 的 ACCENT/BRAIN 旋鑺，同為 Ctrl+F5 生效
