# 對談結束自動記憶與錯誤加權深記

- Scope: global
- Author: holylight
- Confidence: [臨]
- Trigger: 對談結束自動記憶, 錯誤深記, 失敗骨架, deep postmortem, Deep Post-Mortem Gate, session_end flush, 失敗記錄, post-mortem, wisdom_retry, fix_escalation
- Created-at: 2026-06-17
- Related: feedback-memory-system-doc-sync, decisions-architecture, cognitive-patterns, auto-capture碎片sweep污染詞庫-defer根治

## 知識

- [臨] 兩層自動記憶機制（2026-06）：一般知識走品質閘全寫入、失敗加權深記。骨架永遠由 hook 自動寫（小模型填能填的），高 effort 時才加碼喚 Claude 深寫。
- [臨] Stage 1 session_end flush：extract-worker._session_end_writeback 把 session_end 全文萃取 + 累積 knowledge_queue flush 成 [臨] atom。**2026-06-18 修落點路由**（`_flush_route`）：專案 session（cwd 有 project root 且非 ~/.claude）→ scope=shared、落專案層 `{project}/.claude/memory/shared/`（只在該專案注入）；~/.claude / 無 root / 空 cwd → scope=global。修「一律 scope=global、把專案專屬知識污染進 global core 並注入每個專案」缺口（`_flush_item_to_atom` 收 scope/project_cwd/dedup_dir，dedup 對齊落點；守門 verify_flush_routing 4 測 + verify_session_end_flush wiring 2 測）。過品質閘、只清寫成功項（失敗留 queue 重試）、config response_capture.session_end_flush.max_atoms 上限（預設 8）。
- [臨] Stage 2 失敗多區塊骨架：extract-worker._failure_writeback 改寫失敗記錄為五區塊——始末/根因/設計原理/運作邏輯/防再犯。_build_failure_skeleton 把 LLM 敘事填始末、_split_root_cause 從「（根因: …）」尾段拆根因，餘三段留 _FAILURE_TODO_MARK 待補。_failure_dedup_hit 對新骨架始末行 + 舊「- [臨]」單行格式皆去重。
- [臨] Stage 3 Deep Post-Mortem Gate：handlers/stop.py._should_deep_postmortem（純判定）+ gate。觸發＝高 effort 失敗訊號任一：wisdom_retry_count>=2 ∨ fix_escalation_triggered ∨ 同檔 edit_counts>=3。且本 session 未深寫過（deep_postmortem_done 一次性）、block 預算未耗盡（stop_count<stop_gate_max_blocks）、config deep_postmortem.enabled。觸發即 output_block 指示 Claude 結束前用 atom_write 補完整 post-mortem。
- [臨] Stop gate 注入機制＝output_block(reason)（wg_core：{decision:block, reason} + sys.exit）。Deep Post-Mortem Gate 排在 correctness/sync gate 之後（那些優先），共用 stop_blocked_count 預算。
- [臨] 驗證：hooks/verify/verify_failure_skeleton.py（Stage 2）+ verify_deep_postmortem_gate.py（Stage 3，含 handle_stop 端到端 monkeypatch）+ verify_session_end_flush.py（Stage 1）。連字號檔名 extract-worker.py 用 importlib 載入測。跑 python -m pytest hooks/verify/ -q。
- [臨] 自驗發現（dogfood, 2026-06-17）：Deep Post-Mortem Gate 在「本機制自身的開發 session」即誤觸——該 session 零失敗（559 tests 綠、乾淨 commit f11d027），卻因 same_file_3x（extract-worker.py 編輯 4 次）＋ wisdom_retry_count（同 .py 反覆 edit 也累加）觸發。根因：三個 OR 觸發訊號把「高 effort / 多次迭代開發」與「反覆修不好的失敗」混為一談；same_file_3x 與 wisdom_retry 對正常功能開發本就會 ≥ 門檻。建議：postmortem 觸發應 AND 一個「真失敗」訊號（failing_tests 非空 ∨ evasion_flag ∨ 未宣告完成），而非純 effort 計數。待 user 裁決是否收敛觸發條件。
- [臨] 採行方案 A（2026-06-17 同 session 修正、非新 session）：_should_deep_postmortem 由「純 effort 三 OR」改為 (effort) AND (real_failure：failing_tests 非空 ∨ evasion_flag ∨ 未宣告完成)。簽名加 claims_done 參數（caller 算 bool(last_text and claims_completion(last_text)) 傳入，保持純函式、不在 helper 讀 transcript）。新增測 test_effort_but_success_not_triggered 鎖「成功多次迭代開發不誤觸」+ effort×failing/evasion 縱宣告完成仍觸。run_verify 563 passed 零回歸。決策：user 考量「核心功能不靠轉述」選同 session 做（高保真）而非 handoff。

## 行動

- 改失敗 writeback / session_end flush → 同步 extract-worker.py + 對應 verify
- 改 Stop gate → 同步 handlers/stop.py + config.json deep_postmortem + verify_deep_postmortem_gate
- 高 effort 失敗 session 結束前：依 Deep Post-Mortem Gate 指令用 atom_write 補完整 post-mortem
