# [AI] 獨立拍板稿（未看 Codex #7 前寫）

Gate：零幻覺／錯誤零容忍——「錯哪裡」必須有檔:行或實跑數字，否則不動。
權重（我的判定）：立即有效 0.30、自我迭代 0.25、不飄移 0.20、體貼親和 0.15、不 zombie 0.10。每維 0–3 分，加權滿分 3.0。≥2.0 做；1.4–2.0 延後或縮小；<1.4 砍。

| # | 項目 | 錯哪裡（白話＋數字） | 改什麼 | 具體成效 | 立即 | 迭代 | 飄移 | 親和 | zombie | 加權 | 決定 |
|---|------|---------------------|--------|---------|------|------|------|------|--------|------|------|
| D1 | recall-miss 護欄 | 失念偵測在 SessionEnd 遇到字串型 queue 直接炸，08-26～09-18 炸了 16 次，等於這功能死了三週沒人知 | `wg_recall_miss.py:90` 加型別判斷 | 失念偵測恢復；週報「失念」列有真數字 | 3 | 2 | 3 | 2 | 3 | 2.6 | 做 |
| D2 | 歸因取文排除 Agent prompt／通知邊界 | 派 sub-agent 時 atom 原文被塞進 prompt，Stop 又把 prompt 攤平比對 → 那些 atom 必判「有用」；task-notification 被當回合邊界 | `wg_evasion.get_current_turn_text` 跳過 Agent/Task 的 prompt 欄（鏡像 wg_rescue 已有規則）；`_is_real_user_prompt` 排除通知 | 派工回合的假「有用」消失；α 不再被自己餵 | 3 | 3 | 3 | 2 | 3 | 2.8 | 做 |
| D3 | retry 計數差分 | 一場 session 重試數 ≥2 之後，每個回合都被判「失敗」，所有被判用的 atom 一起扣分 | `_detect_turn_outcome` 用本回合增量 | β 不再整場連帶灌爆 | 3 | 3 | 3 | 2 | 3 | 2.8 | 做 |
| D4 | 主／子代理重複計數去重 | 同一 atom 同一回合被主回合與子代理各記一次 → n 虛胖、同 session 三回合就達晉升門檻 | Stop 端以 (atom, turn) 去重；不改統計單位 | n 恢復真實；晉升不再靠一場 session 刷出來 | 2 | 3 | 3 | 1 | 3 | 2.45 | 做 |
| D5 | forget 保護共用判定 | 遺忘候選只保護兩個名字，decisions*/workflow-*/feedback-* 前綴沒擋；目前 dry-run 所以還沒誤殺 | `select_forget_candidates` 改用 `is_core_protected_name()` | 未來開啟 forget 不會殺核心卡 | 1 | 2 | 3 | 1 | 3 | 1.75 | 做（一行，順手） |
| D6 | 健檢 episodic 掃專案層＋heartbeat＋跳過原因 | 週報說 episodic 停擺，實際 09-18 產出在 TSLG 專案層、C:\Projects 有 113 檔；唯讀 session 跳過原因沒 log，事後查不到 | health-weekly 併計專案層；活性讀既有 promotion heartbeat；跳過原因寫不受 debug 開關吞的紀錄 | 週報不再誤報；下次真停擺能查 | 3 | 1 | 3 | 3 | 3 | 2.55 | 做 |
| D7 | SessionStart 例外浮出＋測試 Logs 隔離 | 三個啟動提醒 crash 三週沒人看見（只進 debug log）；pytest 把假資料寫進正式 Logs，統計全被污染 | 例外印 stderr／statusline；verify 用 tmp Logs | 可觀測性鐵律歸位；遙測可信 | 2 | 1 | 3 | 3 | 3 | 2.3 | 做 |
| D8 | hint 停寫／佔位列改計數／cross_session 關 | 晉升稽核檔 94% 是沒人讀的 hint；裁判紀錄 89% 是「案卷未組」佔位；cross_session 軌 90 天 3 筆但開關還開 | 只停 hint 支線（同函式的 ReadHits 保留）；佔位改計數；config 關 | 稽核檔只剩真事件；讀報表的人不被噪音淹 | 2 | 1 | 3 | 3 | 3 | 2.3 | 做（cross_session 在版控 config，其他機器 pull 即同步，無需問） |
| D9 | 文件三處修正 | TECH 說全量萃取在跑（沒跑）、說 SessionEnd 不可調（可到 60s）、說 always-load 1,500–2,000（估 ~5,459） | 改 TECH §2.1/§6.3/§11、Architecture 矛盾段 | 下次診斷不走錯路 | 2 | 0 | 3 | 3 | 3 | 1.95 | 做（低成本、零風險） |
| D10 | R1 記帳單一入口 | 注入記帳在裁切前提交：被砍掉的卡仍算「已注入」→ 去重、效用歸因、失念偵測全吃錯資料；Guardian 尾訊息會被裁掉；路標不計費讓 1,200 硬頂不硬 | 最終送達提交入口；裁切後才記；按完整輸出計費；記錄實際字元數 | 所有下游統計的分母歸真；Guardian 訊息不再消失 | 3 | 3 | 3 | 3 | 2 | 2.9 | 做 |
| D11 | R5 對齊評估器 | 現在的回歸測試跑的不是線上排序，34 題目標已不存在，退步也不會失敗——任何排序調整都驗不到 | 固定快照呼叫真搜尋函式，三層輸出，退步回非零 | 之後每個排序／注入改動都有可重播的數字 | 2 | 3 | 3 | 1 | 3 | 2.4 | 做 |
| D12 | R3 DF 接線／門檻／送出節錄／rescue／標註集 | 「有用」判定用「共享 2 個詞」就算，中文兩字詞太容易撞；IDF 過濾寫好沒接；比對整張卡而非實際送出的那幾行；合成負控制 59/579 誤判 → 接線後 9/579 | 接 build_atom_df、rare_token 3、只比送出節錄、rescue 當佐證、建 20–40 條標註集量誤判／漏判 | 晉升／降級輸入變乾淨；能報出真實錯判率 | 2 | 3 | 3 | 2 | 2 | 2.45 | 做（依賴 D10） |
| D13 | R2 activation 增益實驗 | 排序公式乘上 activation 讓「以前常被注入」壓過「跟這題相關」；離線重播 gain=0 R@1 41.7→80.0 | 對齊評估集上分開測 gain 0.25／0／限幅；採用規則：R@1 與送達率升、負例誤注入不升 → 採 | 該題相關的卡排前面 | 3 | 2 | 3 | 3 | 3 | 2.8 | 做實驗；採用由數據自動決定，不問使用者 |
| D14 | R4 Supersedes 全路徑 | 被新卡取代的舊卡會被 Related 從後門帶回 | 從有效索引池建取代關係，直接檢索／Related／輸出共用；留歷史查詢例外 | 過時知識不再回流 | 2 | 3 | 3 | 3 | 2 | 2.65 | 做 |
| D15 | /extract 路徑 A 修 | 手動 `/extract` 傳給 worker 的 JSON 缺兩個必要欄位 → 永遠空結果卻不報錯 | 修 SKILL 指令或移除該路徑，留對話整理路徑 | 用 /extract 不再白跑 | 2 | 2 | 3 | 3 | 3 | 2.5 | 做 |
| D16 | skill 稽核器契約 | 21 個 skill 被稽核判 19 個失敗，全因把本機自訂欄位當必填；frontmatter 解析不吃多行 | 官方錯誤與本機建議分開、欄位非必填、解析修 | 稽核結果可信，不再誘導亂補欄位 | 2 | 1 | 3 | 2 | 3 | 2.1 | 做 |
| D17 | active skill 寫入契約與固定儀式 | read-project 把可再生目錄寫成 atom 還要手改 MEMORY；consciousness-stream 依風險詞寫推測；fix-escalation 固定 9 次代理；skill-creator 無條件問四題 | 導覽放文件、推測不迴寫、儀式改依證據擴大、訪談先填已知 | 記憶庫不進垃圾；skill 少打擾 | 2 | 2 | 3 | 3 | 3 | 2.55 | 做 |
| D18 | memory skill 矛盾／Architecture 收斂 | 同一 skill 一處說直接 health、一處說先列選單；`--self-iterate` 不存在；Architecture 自稱索引卻與 TECH 打架 | 統一指令；Architecture 只留入口 | 少一個錯誤分支 | 1 | 0 | 3 | 2 | 3 | 1.5 | 做（併 D9 同批文件） |
| D19 | fork 去重／prompt_cache／孤兒清理／provenance／worker log | fork 子代理繼承父對話卻再注一次 atom（700 tok）；cache 命中率沒顯示；3 個 helper 零呼叫；failure atom 指不回原句；worker 死活不可分 | 各自小改 | 少重複注入；看得到 cache；可追溯 | 2 | 1 | 2 | 2 | 3 | 1.9 | 做（Phase 3 小項合批） |
| D20 | skill 可見性裁剪 | 17 個 skill 60 天 0 呼叫，描述每 prompt 佔 ~730 tok（估）；但 9 個已是手動限定 | 先跑 /skill-doctor 出真數字，再用 skillOverrides 對「零使用且非 hook 引用」的 10 個設 disable-model-invocation；檔案不動 | 每 prompt 少幾百 tok；要用時 /名字 照叫 | 2 | 0 | 3 | 2 | 3 | 1.85 | 做（可回滾；settings 在版控會同步同事——但只是把手動觸發變預設，不影響功能） |
| D21 | AEC-Pending 降 dormant | 30 天真觸發 0，但與 DeferralGate 讀的訊號不同，零觸發不證明多餘 | 暫不動 | — | 0 | 0 | 2 | 1 | 2 | 0.75 | 暫緩 |
| D22 | PostToolUse 併二 | 一次 Edit 起四支 Python（各 ~300ms 啟動，但可能並行），收益未量 | 延後；先量真實延遲 | — | 1 | 0 | 2 | 2 | 1 | 1.15 | 延後 |
| D23 | constraint pinning 缺口驗證 | 論文說 compact 後軟規則掉 50pp；但本機 CLAUDE.md 已 @import IDENTITY 且官方會重讀 | 先做一次 compact 前後三條硬契約是否還在的實測；有缺口才釘 | 若有缺口：≤50 tok 保住行為契約 | 1 | 1 | 3 | 2 | 3 | 1.75 | 做驗證（不預設實作） |
| D24 | 棄權閘規則版 | 30% 回合撞預算頂、40% 候選被砍，卻沒有「這題不該注入」的判斷 | 意圖閘→IDF 閘→margin 門檻，對齊評估集量誤注入率／送達率；不升則不上線 | 不相關的 prompt 不再塞卡 | 2 | 3 | 3 | 3 | 2 | 2.65 | 做實驗（依賴 D10/D11/D13） |
| D25 | Related 三組對照 | Related 擴散只看歷史 activation 不看這題；不知它是幫忙還是添亂 | 關閉／現況／傳播+gate 三組跑對齊評估集；關閉不差就砍 | 少一個噪音源或確認它有用 | 2 | 2 | 3 | 2 | 3 | 2.35 | 做實驗（依賴 D11） |
| D26 | TEPA／Hindsight 撤銷 | 本機沒有 conflict_key、沒有同鍵反證來源；α 語意不同 | 等 D12 標註集有數字後再設計 | — | 0 | 3 | 2 | 1 | 1 | 1.4 | 延後 |
| D27 | Codex 裁判去留 | 34 次裁判 0 pass、人工標註 2 筆——資料證明不了有用也證明不了沒用 | 不動；Phase 2 起我在每次裁判後補一筆「裁判說對／說錯」的判定進 human_label（我來標，不麻煩使用者），累到 20 筆再看 | — | 0 | 1 | 2 | 2 | 3 | 1.25 | 不動、累資料 |

## 只有 [使用者] 才能決定的
- 掃過一輪：**沒有**。多機三項（cross_session config、skillOverrides、孤兒 helper）都在版控裡，同步即生效、可回滾，且不改任何人的功能。Codex 裁判去留改成「累到 20 筆判定再看」由我執行。
- 唯一要 [使用者] 做的事：Phase 1a 完成後下「上GIT」。

## 我判斷 Codex 可能被帶偏／我自己可能被帶偏的地方（待對照）
- 我可能高估論文類（D24/D25）的價值——它們是實驗，不是修 bug；已用「依賴＋停止條件」壓住。
- Codex 可能低估「體貼親和」維度（D6/D7/D8 對使用者可感知的噪音）——這是我的強項，若它評低我保留。
